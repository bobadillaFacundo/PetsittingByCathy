# Desarrollo local: proxy Vite usa /api → localhost:8000 (ver vite.config.js)
# No definir VITE_API_BASE=/api en Vercel: el rewrite devuelve HTML y rompe el login.
# En producción el frontend usa https://petsittingbycathy.onrender.com directamente.
