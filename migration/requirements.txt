# Dependencias Python para el backup en el servidor
# (carpeta migration/)
#
# Instalación:
#   python3 -m pip install -r migration/requirements.txt
#
# Además (sistema, no pip):
#   sudo apt update
#   sudo apt install -y postgresql-client   # provee pg_dump

requests>=2.31.0
python-dotenv>=1.0.0
qdrant-client>=1.7.0
chromadb>=0.4.22
