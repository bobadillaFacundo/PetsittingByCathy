# Variables de entorno necesarias para la aplicación
# Copia este archivo como .env y completa los valores

# Base de datos MongoDB
MONGO_URI=mongodb://localhost:27017/guarderiaCanina

# Puerto del servidor
PORT=5000

# Clave secreta para JWT (cambia esto por una clave segura)
JWT=tu_clave_secreta_jwt_aqui_cambiala_por_algo_seguro

# URL del frontend
FRONTEND_URL=http://localhost:5000

# Variables opcionales para Cronofy (calendario)
CLIENT_ID=tu_client_id_cronofy
CLIENT_SECRET=tu_client_secret_cronofy
ACCESS_TOKEN=tu_access_token_cronofy
DATA_CENTER=tu_data_center_cronofy

# Variables opcionales para Supabase
SUPABASE_KEY=tu_supabase_key
SUPABSE_URL=tu_supabase_url 