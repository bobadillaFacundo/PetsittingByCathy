[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: pruebas unitarias sin HTTP
    api: pruebas de endpoints FastAPI
    slow: pruebas lentas o con mocks pesados
